# 회원 관리 종합판 (Interface · Collections · Generics · Enum · try-catch)

> 앞서 배운 것들을 **모두 합칩니다.** 인터페이스로 회원 등급을 구조화하고,
> 컬렉션·제네릭·열거형·예외 처리로 현대적으로 다시 씁니다.
> 핵심은 **`List<Member>`(인터페이스 타입)에 일반·VIP를 섞어 담는 다형성**이에요.
> **아래 Step을 순서대로 따라가면, 마지막 Step에서 완성됩니다.**
>
> 💡 각 Step의 **힌트는 접혀 있어요.** 먼저 스스로 해보고, 막히면 "힌트 보기"를 펼치세요.

---

## 0. 먼저 알아둘 점

이 과제는 다섯 가지를 한 번에 적용해요: **인터페이스 · 컬렉션 · 제네릭 · enum · try-catch.**
인터페이스가 의미를 가지도록 **회원 등급(일반/VIP)** 을 둡니다. 등급마다 혜택이 달라서, `Member` 인터페이스를 두고 `NormalMember`/`VipMember`가 각자 구현해요.

> 솔직 노트: 인터페이스라 이름·이메일 같은 **공통 필드가 두 구현체에 중복**돼요(추상 클래스라면 한 번만). 대신 공통 출력은 인터페이스의 **`default` 메서드**로 한곳에 모아 중복을 줄입니다.

---

## 1. 무엇을 만드나요?

회원을 **일반/VIP 등급**으로 추가·조회·수정·삭제합니다. `List<Member>`에 등급이 섞여 담기고, 출력 시 각 등급의 혜택이 알아서 다르게 나와요.

```
[1]Lite:10 [2]Basic:20 [3]Premium:30
> 2
[1]추가 ... > 1
등급 [1]일반 [2]VIP > 2
이름 > 홍길동 ...
...
> 4 (전체조회)
[VIP] 홍길동 / hong@a.com / 010-1111 (혜택: 10% 할인 + 무료배송)
[일반] 김철수 / kim@b.com / 010-2222 (혜택: 기본 서비스)
```

---

## 2. 학습 목표

| 개념 | 어디서 배우나 |
|------|------|
| 인터페이스 + default | `Member` 규약 + 공통 `printInfo` |
| 다형성 | `List<Member>`에 일반·VIP를 섞어 담기 |
| 컬렉션 (List) | 배열 → `ArrayList` |
| 제네릭 | `List<Member>`로 타입 안전 |
| enum | 요금제(정원) |
| try-catch | 잘못된 입력 처리 |

---

## 3. 핵심 개념

### (1) Member 인터페이스 + default 메서드
모든 회원이 갖춰야 할 메서드를 인터페이스로 정하고, 공통 출력은 `default`로 둡니다.
```java
public interface Member {
    String getName();  String getEmail();  String getPhone();
    String getGrade(); String getBenefit();
    void update(String name, String email, String phone);

    default void printInfo() {   // 다른 메서드만 써서 구현 → 인터페이스에 둘 수 있음
        System.out.println("[" + getGrade() + "] " + getName() + " / "
            + getEmail() + " / " + getPhone() + " (혜택: " + getBenefit() + ")");
    }
}
```

### (2) 인터페이스 + 컬렉션 = 다형성
`List<Member>`는 "Member를 구현한 무엇이든" 담을 수 있어요. 그래서 **일반 회원과 VIP 회원을 한 리스트에** 넣고, 똑같이 다룹니다.
```java
List<Member> members = new ArrayList<>();
members.add(new NormalMember(...));
members.add(new VipMember(...));
for (Member m : members) m.printInfo();   // 각자 등급/혜택이 알아서 다르게 출력!
```
여기서 **제네릭(`<Member>`)** 이 타입을 보장하고, **인터페이스**가 다형성을 가능하게 하고, **컬렉션**이 여러 개를 담아요. 세 개가 한자리에 모이는 지점이에요.

### (3) enum (요금제)
요금제는 정원을 가진 enum으로. `PricePlan.BASIC.getCapacity()` → 20.

### (4) try-catch (입력)
숫자 자리에 문자를 입력해도 안 죽도록 입력을 `try-catch`로 감쌉니다.

---

## 4. 파일 구조

| 파일 | 역할 |
|------|------|
| `Member.java` | **인터페이스** (+ `default printInfo`) |
| `NormalMember.java` / `VipMember.java` | 등급별 구현체 |
| `PricePlan.java` | 요금제 enum (정원) |
| `MemberManager.java` | `List<Member>` 저장소 + CRUD |
| `Main.java` | 요금제 + 메뉴 루프 (try-catch) |

> import: `java.util.List`, `java.util.ArrayList`, `java.util.Scanner`

---

## 5. Step by Step

각 Step에 **목표 / 할 일 / 힌트(접힘) / 확인**이 있습니다.

---

### Step 1. Member 인터페이스 (`Member.java`)

**목표**: 회원이 갖출 메서드를 인터페이스로 정하고, 공통 출력은 `default`로 둔다.

<details>
<summary>💡 힌트 보기</summary>

```java
public interface Member {
    String getName();
    String getEmail();
    String getPhone();
    String getGrade();
    String getBenefit();
    void update(String name, String email, String phone);

    default void printInfo() {
        System.out.println("[" + getGrade() + "] " + getName() + " / "
            + getEmail() + " / " + getPhone() + " (혜택: " + getBenefit() + ")");
    }
}
```

</details>

**확인**: 컴파일 OK.

---

### Step 2. 등급 구현체 (`NormalMember.java` / `VipMember.java`)

**목표**: `Member`를 구현한 일반/VIP 회원을 만든다. (등급/혜택만 다름)

<details>
<summary>💡 힌트 보기</summary>

```java
public class NormalMember implements Member {
    private String name, email, phone;
    public NormalMember(String name, String email, String phone) {
        this.name = name; this.email = email; this.phone = phone;
    }
    @Override public String getName()  { return name; }
    @Override public String getEmail() { return email; }
    @Override public String getPhone() { return phone; }
    @Override public String getGrade()   { return "일반"; }
    @Override public String getBenefit() { return "기본 서비스"; }
    @Override public void update(String name, String email, String phone) {
        this.name = name; this.email = email; this.phone = phone;
    }
}
```

`VipMember`는 거의 똑같고 `getGrade()` → `"VIP"`, `getBenefit()` → `"10% 할인 + 무료배송"` 만 다릅니다. (이 중복이 인터페이스의 한계예요.)

</details>

**확인**: 각 등급의 `printInfo()`가 다르게 나오면 OK.

---

### Step 3. 요금제 enum (`PricePlan.java`)

**목표**: 요금제를 정원을 가진 enum으로 만든다.

<details>
<summary>💡 힌트 보기</summary>

```java
public enum PricePlan {
    LITE(10), BASIC(20), PREMIUM(30);
    private final int capacity;
    PricePlan(int capacity) { this.capacity = capacity; }
    public int getCapacity() { return capacity; }

    public static PricePlan from(int choice) {
        switch (choice) {
            case 1: return LITE;
            case 2: return BASIC;
            case 3: return PREMIUM;
            default: return null;
        }
    }
}
```

</details>

**확인**: `PricePlan.BASIC.getCapacity()`가 20이면 OK.

---

### Step 4. 저장소 + 추가 (`MemberManager.java`)

**목표**: `List<Member>`에 등급 섞인 회원들을 담는다.

<details>
<summary>💡 힌트 보기</summary>

```java
import java.util.ArrayList;
import java.util.List;

public class MemberManager {
    private final List<Member> members = new ArrayList<>();   // 인터페이스 타입 + 제네릭
    private final int capacity;
    public MemberManager(int capacity) { this.capacity = capacity; }

    public boolean isFull() { return members.size() >= capacity; }

    public boolean existsEmail(String email) {
        for (Member m : members) if (m.getEmail().equals(email)) return true;
        return false;
    }
    public void add(Member m) { members.add(m); }   // 일반이든 VIP든 Member로 추가!

    public int size()     { return members.size(); }
    public int capacity() { return capacity; }
}
```

`add(Member m)` 하나로 일반·VIP를 모두 받아요. 이게 인터페이스 + 다형성의 힘이에요.

</details>

**확인**: 추가 후 `size()`가 늘면 OK.

---

### Step 5. 조회 / 전체조회 / 수정 / 삭제 (`MemberManager.java`)

**목표**: List 메서드로 찾고, 출력하고, 수정·삭제한다.

<details>
<summary>💡 힌트 보기</summary>

```java
public Member findByEmail(String email) {
    for (Member m : members) if (m.getEmail().equals(email)) return m;
    return null;
}
public Member findByName(String name) {
    for (Member m : members) if (m.getName().equals(name)) return m;
    return null;
}
public void printAll() {
    if (members.isEmpty()) { System.out.println("등록된 회원이 없습니다."); return; }
    for (Member m : members) m.printInfo();   // 다형성! 등급별로 다르게 출력
}
public boolean update(String email, String name, String newEmail, String phone) {
    Member m = findByEmail(email);
    if (m == null) return false;
    m.update(name, newEmail, phone);
    return true;
}
public boolean delete(String email) {
    Member m = findByEmail(email);
    if (m == null) return false;
    members.remove(m);    // 당기기·null 자동!
    return true;
}
```

</details>

**확인**: 전체조회에서 일반/VIP가 각자 다른 혜택으로 출력되고, 삭제 후 목록이 빈칸 없이 이어지면 OK.

---

### Step 6. 입력을 안전하게 — try-catch (`Main.java`)

**목표**: 숫자 자리에 문자를 입력해도 안 죽게.

<details>
<summary>💡 힌트 보기</summary>

```java
static int readInt(Scanner sc) {
    try {
        return Integer.parseInt(sc.nextLine().trim());
    } catch (NumberFormatException e) {
        return -1;   // 잘못된 입력
    }
}
```

`Scanner`는 하나만 만들어 공유하고 `nextLine()`으로 읽으면 입력 꼬임도 함께 피해요.

</details>

**확인**: 메뉴에서 `abc`를 입력해도 안내 후 계속 돌면 OK.

---

### Step 7. main 통합 + 마무리 (`Main.java`)

**목표**: 요금제(enum) 선택 → 메뉴 루프. 추가 시 등급을 골라 알맞은 구현체를 만든다.

<details>
<summary>💡 힌트 보기</summary>

```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        System.out.println("[1]Lite:10 [2]Basic:20 [3]Premium:30");
        PricePlan plan = null;
        while (plan == null) {
            plan = PricePlan.from(readInt(sc));
            if (plan == null) System.out.println("1~3 중에서 선택하세요.");
        }
        MemberManager manager = new MemberManager(plan.getCapacity());

        while (true) {
            System.out.println("\n[현재 " + manager.size() + "/" + manager.capacity() + "]");
            System.out.println("[1]추가 [2]메일조회 [3]이름조회 [4]전체 [5]수정 [6]삭제 [7]종료");
            int menu = readInt(sc);

            switch (menu) {
                case 1: {   // 추가
                    if (manager.isFull()) { System.out.println("정원이 찼습니다."); break; }
                    System.out.println("등급 [1]일반 [2]VIP");
                    int grade = readInt(sc);
                    System.out.print("이름 > ");   String name  = sc.nextLine();
                    System.out.print("이메일 > "); String email = sc.nextLine();
                    System.out.print("연락처 > "); String phone = sc.nextLine();
                    if (manager.existsEmail(email)) { System.out.println("이미 있는 회원입니다."); break; }
                    Member m = (grade == 2)
                            ? new VipMember(name, email, phone)
                            : new NormalMember(name, email, phone);
                    manager.add(m);   // 다형성: Member로 추가
                    System.out.println("추가되었습니다.");
                    break;
                }
                // case 2~6: findByEmail/findByName/printAll/update/delete 호출
                case 7: System.out.println("이용해주셔서 감사합니다."); return;
                default: System.out.println("1~7 중에서 선택하세요.");
            }
        }
    }
    static int readInt(Scanner sc) {
        try { return Integer.parseInt(sc.nextLine().trim()); }
        catch (NumberFormatException e) { return -1; }
    }
}
```

</details>

**점검 항목**
- [ ] `Member`는 인터페이스이고 `printInfo`만 `default`인가?
- [ ] `List<Member>`에 일반·VIP가 섞여 담기는가? (다형성)
- [ ] 요금제를 enum으로 다루는가?
- [ ] 잘못된 입력에도 안 죽는가? (try-catch)
- [ ] 삭제가 `remove` 한 줄로 되는가?

여기까지 통과하면 **종합판 완성입니다!** 🎉

---

## 6. 학습 체크

- [ ] 인터페이스 + `default` 메서드를 썼다
- [ ] `List<Member>`로 일반·VIP를 섞어 담아 다형성을 활용했다
- [ ] 제네릭으로 타입 안전을 얻었다
- [ ] 요금제를 enum으로 만들었다
- [ ] try-catch로 잘못된 입력을 처리했다

---

## 7. 최종 완성 체크리스트

- [ ] `Member`(interface) / `NormalMember` / `VipMember`
- [ ] `PricePlan`(enum) / `MemberManager`(List) / `Main`(try-catch)
- [ ] 등급 선택 추가 + 전체조회에서 등급별 출력
- [ ] 잘못된 입력에도 안 죽음

---

## 8. (선택) 도전 과제

1. **추상 클래스로 중복 제거**: 인터페이스 대신 `abstract class Member`로 공통 필드를 한곳에 모아, Normal/Vip의 중복을 없애고 비교해 보기
2. **Repository 인터페이스**: `MemberRepository` 인터페이스(add/find/delete...)를 정의하고 `MemberManager`가 구현 → "인터페이스에 맞춰 프로그래밍" 체험
3. **제네릭 직접 만들기**: `Repository<T>` 같은 제네릭 인터페이스/클래스 만들기
4. **Map으로 빠르게**: `Map<String, Member>`(이메일 key)로 조회/삭제를 O(1)로
5. **등급 enum화**: 등급도 `Grade` enum(혜택 포함)으로 만들어 Normal/Vip 구현체를 하나로 합치기
6. **커스텀 예외 + Stream**: `DuplicateEmailException`을 던지고, `members.stream()`으로 조회/정렬